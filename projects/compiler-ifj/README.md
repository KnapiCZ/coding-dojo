
# 🧩 IFJ Projekt – Implementace překladače v C

Implementace překladače pro jazyk IFJ25 (Implementace Formálních Jazyků a Překladačů).  
Projekt je psán **v jazyce C** a obsahuje lexikální, syntaktickou a sémantickou analýzu, tabulku symbolů a generátor cílového kódu.

---

(Generated by ChatGPT, edited by Filip Knapo)
Případné změny se domluvíme na DISCORDU.

---

## 📁 Struktura projektu

/src # Zdrojové soubory (.c, .h)
/tests # Testy a vstupní/output soubory
/doc # Dokumentace, diagramy, návrhy
/examples # Ukázkové vstupy
Makefile # Kompilace projektu
README.md # Tento soubor


Každý `.c` soubor má odpovídající `.h` soubor, kde jsou deklarovány funkce a struktury, které jsou exportovány.

---

## 💡 Pravidla pro čistý C kód

1. **Formátování:**
   - Používej **4 mezery** pro odsazení (ne tabulátory).
   - Maximální délka řádku: **100 znaků**.
   - {} na nových řádcích
    ```c
    if(1==1)
    {
        return 1
    }
    ```
   - Každá funkce má krátký **komentář nad deklarací**:
    ```c
    /**
     * @brief 
     * 
     * @param x 
     * @param a 
     */
    void test(int x, int a)
    {
        return
    }
    ```

2. **Pojmenování:**
   - Proměnné a funkce: `camelCase`
   - Struktury a typy: `PascalCase`
   - Konstanty: `UPPER_CASE`
   - Interní funkce (neexportované) začínají `_`

3. **Kód piš srozumitelně:** komentuj *proč*, ne *co*.

4. **Chyby a hlášky:** jednotně, anglicky např. `ERROR: Lexical error`. (pozdeji kdyztak upresnime)

5. **Nastavený formáter a linter** Clang Format,
    - VSCode Extension - Linter - https://marketplace.visualstudio.com/items?itemName=CS128.cs128-clang-tidy
    - VSCode Extension - Formater - https://marketplace.visualstudio.com/items?itemName=xaver.clang-format
    - !! Je potřeba ještě stáhnout clang formater do pc 
    - Linux(fedora): sudo dnf install clang-tools-extra
    - macOS: brew install clang-format
    - Windows: nainstalovat LLVM a přidat clang-format.exe do PATH

---

## 🔄 Pravidla pro Git a commity

1. **Commity malé a srozumitelné** – jedna logická změna.
2. **Formát commit zprávy:**

<typ>: <stručný popis>

[volitelný delší popis]

Např.:

feat: přidán generátor kódu pro aritmetické výrazy
fix: oprava segfaultu v parseru
refactor: přesun tabulky symbolů do samostatného modulu

3. **Typy commitů:**
- `feat` – nová funkcionalita  
- `fix` – oprava chyby  
- `refactor` – úprava kódu bez změny logiky  
- `test` – přidání nebo úprava testů  
- `docs` – úprava dokumentace  
- `chore` – údržba, konfigurace, Makefile apod.

4. **Před commitem:**
- Projekt se **kompiluje bez chyb** (v pozdejsich fazich)
- Testy projdou (`make test`) (kdyz nejake budou)
- Použij `clang-format a clang-tidy (linter)`

---

## 🧱 Doporučený pracovní postup

1. Vytvoř si větev pro každou větší změnu (`feature/parser`, `fix/lexer-bug` …)
2. Pravidelně merguj změny z `main`.
3. Po dokončení otevři pull request s popisem a testy.
4. Merge s `--no-ff` pro zachování historie.

---

## 🧪 Testování

- Každý modul má vlastní testovací skript.
- Automatické testy:
```bash
make test

    Testovací soubory:

        /tests/input/ – vstupy

        /tests/output/ – očekávané výstupy

⚙️ Kompilace

make        # Kompilace překladače
make run    # Spuštění s testovacím vstupem
make clean  # Vyčištění binárek a objektů

✨ Doporučení navíc

    Používej valgrind pro kontrolu úniků paměti.

    Dokumentuj datové struktury a stavové automaty.

    Nekomituj binárky ani dočasné soubory.

    Pokud si nejsi jistý, napiš komentář nebo se zeptej týmu.

    🧙‍♂️ „Čistý kód je jako dobré kouzlo – funguje, protože mu rozumíš.“


---

## Odkazy, které se můžou hodit
    - https://app.gleek.io (Diagramy psane kodem)
    - https://www.youtube.com/live/ZRmSx-ecB-o (IFJ predstaveni projektu stream)
    - https://moodle.vut.cz/course/view.php?id=294121 (IFJ MOODLE)